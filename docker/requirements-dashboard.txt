# Runtime dependencies for the Streamlit dashboard image (Dockerfile.dashboard).
#
# app/dashboard.py + app/views/* never import src/ (an architectural guardrail)
# and talk to the API over HTTP only (httpx) — confirmed by reading every
# `import`/`from` line in app/views/*.py. No scikit-learn, scipy, xgboost,
# lightgbm, shap, mlflow, or pyarrow is needed to render the dashboard;
# display metadata is read from plain CSVs (pandas.read_csv), never parquet.
#
# Pins are copied by hand from pyproject.toml's `dependencies` array
# (the single source of truth) — keep these two in sync whenever
# pyproject.toml's pins change.
pandas~=2.3.3
streamlit>=1.57,<1.60
httpx~=0.28.1
plotly~=6.7.0
pydantic-settings~=2.14.2
