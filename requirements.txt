# Dependency pins live in pyproject.toml (single source of truth, Decision 020).
# This file is kept so the documented one-command setup keeps working:
#
#     pip install -r requirements.txt      # editable install + dev tools
#
# Runtime-only (no pytest/ruff/notebook):  pip install -e .
-e .[dev]
